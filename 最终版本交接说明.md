# 矩阵乘法 SoC 最终版本交接说明
## 摘要

最终版本先用双输入槽、双结果槽把 ExtRAM 读取、8-lane 矩阵计算和双 word CRC 构造成重叠流水线，再针对 50 MHz 单 word 读取约 3.2 ms 的理论瓶颈，引入 62.5 MHz、每地址保持完整 16 ns 的 ExtRAM 快读，并通过基于 Block RAM、Gray 指针和双级同步器的 512×64 异步 FIFO 安全跨越 FAST/SYS 时钟域，将理论输入读取时间降至约 2.56 ms；最终实现保持 66-bit 结果精度、正确的 CRC 数据顺序和 DSP=0，且静态实现通过了 CI 的 setup/hold 检查。
## 1. 文档目的与版本基准

本文用于项目归档和人员交接，说明最终版本的工程范围、完整工作链路、关键模块、核心优化方法、验证结果以及后续复现时需要注意的事项。

本文描述以最终提交代码为准，而不是以中间实验记录为准。

| 项目 | 最终信息 |
| --- | --- |
| 工程目录 | 本 Git 仓库根目录 |
| Git 分支 | `7.15` |
| 最终 RTL 代码基准 | `ca84ca9102496e1f8a39b286ddc013676e1295b2` |
| 基准简称 | `ca84ca9` |
| 基准提交说明 | `perf: switch fast reader to 62.5 MHz at runtime` |
| GitLab CI 流水线 | `15281` |
| CI 结果 | fresh Vivado 编译成功，bitstream 已生成 |

最终提交直接修改了以下三个 RTL 文件：

- `rtl/soc_top.v`
- `rtl/ip/matmul/matmul_axi_slave.v`
- `rtl/ip/ram_wrap/axi_wrap_ram_sp_external.v`

仓库中的 `.local_build/` 是本地完整发布包及仿真副本，`ci_data/` 是 CI 信息采集结果；二者均未纳入最终 Git 提交，不应作为最终源码版本的判定依据。

---

## 2. 任务与数据模型

系统连续处理 5000 组无符号 4×4 矩阵乘法：

```text
C = A × B
```

每组输入包含：

- 矩阵 A：16 个 32-bit 无符号元素；
- 矩阵 B：16 个 32-bit 无符号元素；
- 合计 32 个 32-bit word，即 128 byte。

每个输出元素为四个 32-bit 无符号乘积之和，使用 66-bit 保存：

```text
C[i][j] = A[i][0]×B[0][j]
        + A[i][1]×B[1][j]
        + A[i][2]×B[2][j]
        + A[i][3]×B[3][j]
```

每个 66-bit 结果按以下顺序编码为三个 32-bit word：

1. 低 32 位；
2. 中间 32 位；
3. 最高 2 位零扩展到 32 位。

因此每组输出形成 16×3=48 个 32-bit word。最终版本不依赖把结果写回 ExtRAM，而是按相同的逻辑结果字节流直接计算 CRC32。

---

## 3. 最终系统架构

最终版本划分为两个主要时钟域：

- **FAST 域**：运行时 62.5 MHz，负责 ExtRAM 连续读取、相邻 word 打包以及异步 FIFO 写入；
- **SYS 域**：50 MHz，负责 FIFO 读取、输入双缓冲、矩阵计算、结果双缓冲、CRC32 和 UART 输出。

完整数据链路如下：

```text
复位释放
   │
   ├─ MMCM 初始化并通过 DRP 将快读时钟切换为 62.5 MHz
   │
   ├─ 硬件批处理自动启动（源地址 0x1c40_0000，5000 组）
   │
   ├─ UART 发送固定前缀
   │    MATMUL_START\n
   │    MATMUL_CRC32=
   │
   ▼
ExtRAM（32-bit）
   │  FAST 域每 16 ns 推进一个地址
   ▼
相邻两个 32-bit word 组成一个 64-bit pair
   │
   ▼
512×64 双时钟异步 FIFO（Block RAM）
   │  ready/valid
   ▼
双输入槽 A/B（SYS 域）
   │
   ▼
8-lane、16 bit/拍、66-bit 累加矩阵乘法核心
   │
   ▼
双结果槽 C（每槽 48 word）
   │
   ▼
2 word/拍 CRC32 数据流
   │
   ▼
UART 发送 8 位 CRC、换行和 MATMUL_DONE\n
```

稳态工作时，多个阶段并行运行：

```text
FAST 域：读取后续输入并写入 FIFO
SYS 域输入级：从 FIFO 填充下一组输入槽
SYS 域计算级：计算当前组矩阵
SYS 域 CRC 级：处理上一组结果
```

系统性能来自整条数据通路的重叠，而不是只依靠某一个计算模块的峰值速度。

---

## 4. 启动、时钟与复位链路

### 4.1 最终启动方式

`matmul_axi_slave.v` 在复位后置位 `auto_start_pending`。进入正常工作状态后的第一个 SYS 时钟周期，模块调用批处理启动逻辑，锁存：

- `AUTO_SRC_BASE = 0x1c40_0000`；
- `AUTO_GROUP_COUNT = 5000`；
- CRC 初值 `0xffff_ffff`；
- 输入、输出和 CRC 流水状态。

最终 `sdk/software/examples/asm/user-sample.c` 的 `main()` 是空循环。因此最终批处理、CRC 和 UART 标识均由硬件完成；CPU、AXI slave 寄存器和 AXI master 端口仍保留在 SoC 中，可用于寄存器访问、单组操作或后续扩展，但不处于最终批处理关键路径上。

### 4.2 62.5 MHz 快读时钟

`soc_top.v` 使用 `MMCME2_ADV` 和两个 `BUFG` 构造快读时钟及反馈路径。

硬件启动时，MMCM 的静态参数为：

```text
输入时钟：50 MHz
CLKFBOUT_MULT_F：20
DIVCLK_DIVIDE：1
CLKOUT0_DIVIDE_F：20
```

MMCM 首次锁定后，SYS 域 DRP 状态机执行：

1. 复位 MMCM；
2. 写 PowerReg `0x28 = 0xffff`；
3. 读取 CLKOUT0 配置寄存器 `0x08`；
4. 保留必要的保留位，将 CLKOUT0 分频参数改为 16；
5. 解除 MMCM 复位；
6. 等待 MMCM 再次锁定；
7. 置位 `matmul_fast_drp_done`。

VCO 频率为 1 GHz，输出分频由 20 改为 16 后：

```text
1 GHz ÷ 16 = 62.5 MHz
```

只有 `matmul_fast_mmcm_locked` 和 `matmul_fast_drp_done` 同时有效，FAST 域复位才会释放，保证 ExtRAM 快读逻辑不会在时钟稳定前启动。

在 ModelSim 的 `SIMULATION` 分支中，直接生成周期 16 ns 的时钟，以验证 62.5 MHz 数据通路功能。

---

## 5. 第一层优化：读取、计算与 CRC 的流水线化

### 5.1 初始串行结构的限制

如果每组都严格按“读完输入—完成计算—处理结果—再读下一组”的方式运行，各阶段会相互等待：

```text
读取第 N 组 → 计算第 N 组 → CRC 第 N 组 → 读取第 N+1 组
```

此时读存储器时计算单元空闲，计算时存储器接口空闲，CRC 时读取和计算也可能停止。总耗时接近各阶段耗时之和。

最终版本把这条串行链路改造成弹性流水线，使整体吞吐由最慢阶段决定。

### 5.2 双输入槽：计算与下一组预取重叠

模块内部设置两套 A/B 输入寄存器：

```text
a_regs / b_regs
a_regs_alt / b_regs_alt
```

两个槽交替承担“当前计算槽”和“下一组填充槽”：

```text
阶段 0：第 0 组进入 Slot 0
阶段 1：计算第 0 组，同时第 1 组进入 Slot 1
阶段 2：计算第 1 组，同时第 2 组进入 Slot 0
阶段 3：计算第 2 组，同时第 3 组进入 Slot 1
```

每组输入共有 16 个 64-bit pair。SYS 域每次握手接收一个 pair，并把低、高两个 32-bit word 写入对应的 A 或 B 寄存器。`input_ready_valid`、`input_ready_slot` 和 `input_ready_group` 记录已经填充完成、等待计算的下一组。

当当前计算即将结束，且下一组输入已经就绪、CRC 结果槽也可用时，状态机会直接交换输入槽和输出槽并启动下一组计算，减少组间空拍。

### 5.3 双结果槽：计算与 CRC 重叠

结果端同样使用两套 48-word 寄存器：

```text
c_regs
c_regs_alt
```

计算核心把当前组结果写入一个结果槽时，CRC 流可以读取另一个结果槽。`compute_done_pending` 和 `crc_pending_valid` 用于处理短暂的上下游速率差异，防止尚未完成 CRC 的结果槽被下一组计算覆盖。

因此在稳定阶段可以同时存在：

- 第 N+1 组输入正在预取；
- 第 N 组正在计算；
- 第 N-1 组正在计算 CRC。

### 5.4 ready/valid 弹性连接

异步 FIFO 到矩阵模块采用 `direct_ext_pair_valid/direct_ext_pair_ready` 握手：

- FIFO 有完整 pair 时拉高 `valid`；
- SYS 域处于读取状态时拉高 `ready`；
- 只有二者同时有效才消费数据。

这种方式把 FAST 读取节奏与 SYS 计算状态解耦。下游发生短暂停顿时，数据保留在 FIFO 或输出寄存器中，不会丢失、重复或改变顺序。

---

## 6. 第二层优化：62.5 MHz 整周期 ExtRAM 快读

### 6.1 瓶颈分析

5000 组输入共包含：

```text
5000 × 32 = 160000 个 32-bit word
```

在 50 MHz 下，如果每拍读取一个 word，输入读取的理论时间下限为：

```text
160000 × 20 ns = 3.2 ms
```

在前级流水线和计算核心已经加速后，继续增加乘法并行度对总时间改善有限，因为输入数据还没有足够快地到达计算核心。最终优化因此从“继续加宽计算核心”转向“提高 ExtRAM 有效读取带宽”。

### 6.2 整周期快读

FAST 域工作在 62.5 MHz，周期为：

```text
1 ÷ 62.5 MHz = 16 ns
```

读取状态机每个 FAST 周期最多推进一个 ExtRAM word 地址。前一个地址的数据被采样时，同时准备下一个地址：

```text
周期 0：给出地址 A0
周期 1：采样 A0，给出地址 A1
周期 2：采样 A1，给出地址 A2
周期 3：采样 A2，给出地址 A3
```

与半周期或双沿读取不同，每个地址保持完整的 16 ns，不使用地址双沿切换，也不在 8 ns 半周期内强行完成地址建立和数据采样。这样兼顾带宽提升和外部 SRAM 接口稳定性。

新的理论输入读取时间为：

```text
160000 × 16 ns = 2.56 ms
```

相对于 50 MHz 单 word/拍读取，理论上减少约 0.64 ms；更重要的是，它突破了原先约 3.2 ms 的输入带宽下限。

### 6.3 64-bit pair 打包

FAST 域使用 `fast_pair_low` 和 `fast_pair_half` 把相邻两个 32-bit word 组合成一个 64-bit pair：

```text
pair[31:0]  = 低地址 word
pair[63:32] = 高地址 word
```

每组 32 个 word 因而变成 16 个 pair。SYS 域每次接收一个 pair，同时写入两个输入寄存器，降低跨域握手和下游装载控制的频率。

FIFO 满时，FAST 域暂停地址推进并保持当前读取状态，避免数据覆盖。

---

## 7. 异步 FIFO 与跨时钟域设计

### 7.1 FIFO 结构

FAST 域和 SYS 域之间使用 `512×64 bit` 双时钟异步 FIFO：

- 写端时钟：`matmul_fast_clk`，运行时 62.5 MHz；
- 读端时钟：`aclk/sys_clk`，50 MHz；
- 写端只负责同步写；
- 读端只负责同步读；
- 存储数组不复位；
- 控制指针和输出有效状态单独复位。

存储体使用：

```verilog
(* ram_style = "block" *) reg [63:0] mem [0:511];
```

总容量为 32768 bit，目标映射为一个 RAMB36。采用 Block RAM 可以避免用大量触发器实现 FIFO，降低布局布线压力。

### 7.2 Gray 指针同步

读写地址在各自时钟域内使用二进制指针，跨域传输时转换为 Gray 码：

```text
gray = binary XOR (binary >> 1)
```

Gray 码相邻计数值只改变一个 bit，能够降低多位二进制指针同时翻转时被异步采样产生非法组合的风险。

跨域指针均经过两级同步器，并标记 `ASYNC_REG="TRUE"`：

- 读 Gray 指针同步到 FAST 域，用于判断 FIFO 满；
- 写 Gray 指针同步到 SYS 域，用于判断 FIFO 空。

FIFO 满判断采用异步 FIFO 的环回位比较方法，空判断采用同步后的写指针与本地读指针相等。

### 7.3 数据总线跨域原则

64-bit 数据不通过 64 个独立同步器，也不直接通过组合逻辑跨域。数据只由 FAST 时钟写入双口 RAM，再由 SYS 时钟从另一端口同步读出。

跨域同步的主要对象是：

- Gray 编码读写指针；
- 启动、基地址和 word 数等低速控制信息；
- 流错误标志。

这种设计保证一个 64-bit pair 作为整体交付给 SYS 域，避免高低 word 来自不同采样时刻。

---

## 8. 8-lane、16 bit/拍矩阵计算核心

### 8.1 并行粒度

最终核心包含 8 条计算 lane。每轮同时计算相邻两行、四列共 8 个输出元素：

```text
Lane 0..3：第 r 行的 C[r][0..3]
Lane 4..7：第 r+1 行的 C[r+1][0..3]
```

完成前两行后，再计算后两行。因此一组 4×4 输出分成两个 8-lane 批次。

每条 lane 对 k=0..3 的四项乘积进行 66-bit 累加。

### 8.2 无 DSP 的 16-bit 部分积

代码不使用 Verilog `*` 运算符，而是用移位、选择和加法构造无符号乘法，确保实现不占用 DSP。

每条 lane 每拍处理乘数的 16 个 bit：

1. 将 32-bit 被乘数扩展为 66 bit，并按当前 `mul_bit` 左移；
2. 根据乘数当前 16 个 bit 选择对应移位项；
3. 将 16 个部分积按平衡树求和；
4. 下一拍处理乘数高 16 位；
5. 得到完整 32×32 无符号乘积并加入该输出元素的 k 维累加值。

### 8.3 平衡部分积加法树

16 个条件部分积不是简单串联相加，而是先两两求和，再逐级合并：

```text
16 个部分积
  ↓ 两两相加
8 个中间和
  ↓
4 个中间和
  ↓
2 个中间和
  ↓
1 个 66-bit 部分积结果
```

这种结构减少了加法器串联深度，在保持无 DSP 的同时改善组合路径时序。

### 8.4 结果格式化

每个 66-bit 累加结果写入结果槽中的三个连续 word：

```text
word 0 = result[31:0]
word 1 = result[63:32]
word 2 = {30'b0, result[65:64]}
```

矩阵元素按行优先顺序进入 48-word 结果流，为 CRC 模块提供确定的数据次序。

---

## 9. CRC32 流水与 UART 输出

### 9.1 CRC 数据顺序

CRC 使用 IEEE 反射形式多项式 `0xedb88320`：

- 初值：`0xffffffff`；
- 每个 32-bit word 按 `[7:0]`、`[15:8]`、`[23:16]`、`[31:24]` 顺序处理；
- 最终结果与 `0xffffffff` 异或。

CRC 输入严格按照：

```text
group 0 → group 4999
每组 word 0 → word 47
每个 word 内低字节 → 高字节
```

因此它等价于对规定格式的完整逻辑结果区进行 CRC32。

### 9.2 每拍处理两个 word

`crc_stream_data0` 和 `crc_stream_data1` 同时从当前结果槽取相邻两个 word，并串联执行两次 `crc32_update_word`。每组 48 word 只需 24 个 CRC 流周期。

CRC 流处理一个结果槽时，计算核心可以写另一个结果槽。最后一组的最后一对 word 完成后，模块：

1. 锁存最终 CRC；
2. 清除 `busy`；
3. 置位 `done`；
4. 停止批处理和 ExtRAM 快读通路。

### 9.3 UART 输出时序

硬件 UART 使用 50 MHz SYS 时钟和分频值 434，实现约 115200 baud、8N1 字符发送。

复位后立即发送固定前缀：

```text
MATMUL_START\n
MATMUL_CRC32=
```

CRC 完成后发送：

```text
XXXXXXXX\n
MATMUL_DONE\n
```

其中 `XXXXXXXX` 为小写十六进制、最高 nibble 先发。`soc_top.v` 在硬件标识输出活动期间，将顶层 `UART_TX` 复用到矩阵模块的串口发送信号；发送结束后释放硬件 UART 接管。

---

## 10. 核心创新点

### 一：从模块加速转向系统级流水线

设计没有把优化局限在乘法器本身，而是把输入预取、矩阵计算和 CRC 组织成多级并行流水。双输入槽和双结果槽使存储器、计算核心和 CRC 单元同时工作，把组间串行等待转化为局部 ready/valid 背压。

### 二：用带宽模型定位真正瓶颈

通过计算 160000 个输入 word 在 50 MHz 下的 3.2 ms 理论读取下限，并对比不同乘法位宽的仿真结果，确认系统瓶颈已经从计算转移到 ExtRAM 读取。后续优化因此直接针对瓶颈，而不是继续堆叠无收益的计算资源。

### 三：62.5 MHz 整周期 ExtRAM 读取

通过提高地址推进频率，将理论读取时间由 3.2 ms 降到 2.56 ms；同时坚持每个地址保持完整 16 ns，避免半周期双沿方案对外部 SRAM 建立/保持时间的苛刻要求，在性能和可靠性之间取得平衡。

### 四：64-bit pair 与 Block RAM 异步 FIFO

FAST 域将两个连续 word 组合为一个 64-bit pair，再通过 512×64 双时钟 Block RAM FIFO 交给 SYS 域。Gray 指针、双级同步器和同步 RAM 读写共同实现数据完整性与时钟域隔离，FIFO 同时吸收读存储器与计算状态切换之间的瞬时速率差。

### 五：资源约束下的 8-lane 无 DSP 乘法核心

利用 16-bit 部分积和平衡加法树实现 8 条并行 lane，不使用 `*`，最终综合 DSP=0。该结构在并行度、组合深度和资源使用之间形成了可综合、可收敛的折中。

### 六：计算结果直接进入双 word CRC 流

结果按规定的 66-bit 编码写入交替结果槽，并以每拍两个 word 的速度计算 CRC。CRC 顺序与逻辑结果区字节流完全一致，同时避免将 CRC 阶段变成新的串行瓶颈。

### 七：时钟稳定后再开放快读数据通路

MMCM 动态重配置、重新锁定和 FAST 域复位释放被组织成明确的启动序列。快读逻辑只有在最终时钟配置稳定后才运行，降低了启动阶段产生错误地址或错误 FIFO 数据的风险。

---

## 11. 最终模块职责

| 文件 | 主要职责 |
| --- | --- |
| `rtl/soc_top.v` | 实例化矩阵模块；生成并分发 FAST 时钟；执行 MMCM DRP 重配置；同步 FAST 复位；连接 ExtRAM 快读接口和 UART 复用。 |
| `rtl/ip/ram_wrap/axi_wrap_ram_sp_external.v` | 保留原 AXI SRAM wrapper；在矩阵批处理活动时选择快读端口；按 16 ns 周期推进 ExtRAM 地址；两个 word 打包为 pair；实现 512×64 异步 FIFO。 |
| `rtl/ip/matmul/matmul_axi_slave.v` | 自动启动 5000 组任务；管理双输入/双结果槽；执行 8-lane 16-bit 部分积矩阵乘法；按 2 word/拍计算 CRC；输出 UART 标识。 |
| `sdk/software/examples/asm/user-sample.c` | 最终版本中 CPU 进入空循环；保留历史辅助函数，但不参与最终批处理关键路径。 |

矩阵模块的 AXI slave 控制寄存器仍保留，包括 `CTRL`、`STATUS`、`SRC_BASE`、`DST_BASE`、`GROUP_COUNT` 和 `CRC32`。AXI master 读写接口也仍在模块端口中，但最终自动批处理输入由专用 ExtRAM/FIFO 快读链路提供。

---

## 12. 验证结果

### 12.1 本地 ModelSim

10 组回归：

```text
MATMUL_START
MATMUL_CRC32=51b7f2d2
MATMUL_DONE
```

5000 组回归：

```text
MATMUL_START
MATMUL_CRC32=67942d31
MATMUL_DONE
```

5000 组仿真中：

- UART CRC 与独立 expected C 文件计算结果一致；
- CRC 第一个十六进制字符约在 2.563 ms 开始发送；
- 完整 UART 最后一个字符帧约在 4.386 ms 结束；
- FAST 仿真时钟周期为 16 ns；
- 每组消费 16 个 pair，5000 组合计 80000 个 pair。

### 12.2 GitLab CI

流水线 `15281` 对最终 RTL 代码基准 `ca84ca9` 执行了 fresh Vivado 流程：

| 指标 | 结果 |
| --- | ---: |
| WNS | `+0.492 ns` |
| TNS | `0.000 ns` |
| WHS | `+0.061 ns` |
| THS | `0.000 ns` |
| DSP 使用量 | `0` |
| bitstream | 成功生成 |
| `user-sample.bin` | 2448 byte |
| 软件 SHA256 | `906ec1cd55c698855e77617ff845133f0a4fdd85f8e4c8bf7f3002d9a6ee473e` |
| RTL SHA256 | `3c11a0c5aa3916ea6a763661bacbf2b78a34f0f6509f72ddf04382746a23e6ff` |

lint 日志没有真正的 error；日志中出现 `error/axi_err_slave.v` 是文件路径的一部分，不代表 lint 失败。现有 warning 主要来自发布包原有结构及综合提示，未阻止 bitstream 生成。

### 12.3 验证边界

需要特别注意：

- ModelSim 使用直接生成的 62.5 MHz FAST 时钟，验证的是快读、FIFO、计算、CRC 和 UART 数据通路；
- Vivado 静态时钟摘要显示 MMCM 初始配置的 50 MHz 输出，并确认 `MMCME2_ADV`、DRP 端口和数据通路能够综合实现；
- 运行时 DRP 从 50 MHz 切换到 62.5 MHz 的真实行为不能由该 ModelSim 分支直接覆盖；
- 因此最终硬件验收仍应以板上 CRC 稳定匹配和实测时间为准，不应只依据零延迟仿真或静态时序摘要。

---

## 13. 归档与复现建议

### 13.1 源码

归档时应保存：

```text
仓库：本 Git 仓库根目录
分支：7.15
最终 RTL 代码基准：ca84ca9102496e1f8a39b286ddc013676e1295b2
```

### 不应混入最终源码包的内容

- `.local_build/`；
- `ci_data/`；
- Vivado 临时工程、缓存和中间文件；
- ModelSim 临时工作库、VCD 和波形缓存；
- 访问令牌、账号密码或环境变量文件。

### 后续维护的首要检查项

若未来重新编译或移植，建议按以下顺序检查：

1. `MMCME2_ADV` 是否被正确保留并完成 DRP 读写；
2. FAST 域复位是否只在最终锁定后释放；
3. FIFO 是否仍映射为 Block RAM，而不是大量 FDRE；
4. Gray 指针同步器是否保留 `ASYNC_REG` 属性；
5. ExtRAM 地址是否每 16 ns 最多变化一次；
6. FIFO 是否出现 overflow、underflow 或 pair 次序错误；
7. 5000 组是否恰好消费 80000 个 pair；
8. CRC 是否与独立软件计算一致；
9. 综合后 DSP 是否仍为 0；
10. setup 和 hold 是否均为正裕量；
11. 板上多次随机数据评测的 CRC 是否稳定一致。

---
